name: hq_picker
description: "A stateful widget that allows users to pick media files (images, videos, audio, files) from their device."
version: 0.0.11
homepage: "https://github.com/azabcodes/hq_picker"

platforms:
  android:
  ios:

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  photo_manager_image_provider: ^2.2.0
  photo_manager: ^3.11.0
  loading_animation_widget: ^1.3.0
  image_picker: ^1.2.3
  flutter_saver: ^0.0.7
  camerawesome: ^2.5.0
  path: ^1.9.1
  lucide_icons_flutter: ^3.1.15
  native_android_path: ^0.0.5
  file_selector: ^1.1.0
  flutter_image_compress: ^2.5.1
  permission_handler: ^13.0.0
  path_provider: ^2.1.6
  flutter_bloc: ^9.1.1
  equatable: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
